---
name: sus
description: "Alias for `sup` (send-upstream). File an issue or open a PR on an upstream repo (a fork's parent, a dependency, or any external project). Use when asked to 'sus', 'send upstream', 'file upstream issue', 'upstream PR', 'contribute this fix back', or 'report this bug upstream'."
user-invocable: true
allowed-tools:
  - Bash
  - Read
  - Edit
  - Write
---

# SUS — Send to Upstream

This is an alias for the **sup** skill. Read and follow the full
procedure in:

`~/.claude/skills/sup/SKILL.md`

Invoke the sup skill procedure exactly as documented there.
